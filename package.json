{
  "name": "airline-mock-server",
  "version": "1.0.0",
  "description": "An mock server for my-airline",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ayush Jain",
  "license": "ISC"
}
